const num1=2, num2=3;
const sumVal = function(fnum,snum){
console.log(`the sum is:  ${fnum+ snum}`);
}

sumVal(num1,num2)

const multiplyVal = function(fnum,snum){
    console.log(`the multiplecation is:  ${fnum * snum}`);
    }
    console.log("let's test if i will be excuted");
    multiplyVal(num1,num2);

    const person={name:'Bob'}
    console.log(person.name);
